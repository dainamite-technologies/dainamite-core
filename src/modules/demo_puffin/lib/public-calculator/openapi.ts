/**
 * Shared OpenAPI tag for the demo_puffin public calculator routes. Routes
 * import this so the generated `openapi.generated.json` groups them under one
 * heading and external consumers can discover the public surface.
 */

export const PUFFIN_PUBLIC_OPENAPI_TAG = 'Puffin Public Calculator'
