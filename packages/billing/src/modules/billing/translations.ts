/**
 * Translatable fields for billing entities.
 *
 * Phase 0 keeps this empty — the Billing Account name and invoice line
 * descriptions are user-entered free text, not localizable templates.
 * If we ever ship localized invoice templates (Phase 4 admin UI), the
 * template label fields land here.
 */
export const translatableFields: Record<string, string[]> = {}
